<div class="container">
  <div class="jumbotron">
    <h2> <%= @todo.description %> </h2>
    <% if  @todo.completed %>
      <p>Completo: Si</p>
    <% else %>
      <p>Completo: No</p>
    <% end%>
    <div class="float-right">
      <%= link_to 'Volver', todos_path, class: 'btn btn-outline-primary' %>
    </div>

  </div>
</div>
